# PolicyChain Agent 开发规则

## 项目目标与产品边界

PolicyChain 是面向政策研究的 Agentic RAG 系统，目标链路是：

用户输入政策链接或政策正文 -> 读取并校验主政策内容 -> 政策解构与力度判断 -> 相似政策对比 -> 实施路径分析 -> 行业影响分析 -> A 股公司业务匹配。

用户入口不是普通问答。前端和工作流的主输入必须是某个政策链接，或用户复制粘贴的大段政策正文。短问题只能作为兼容旧 CLI 测试或内部调试的兜底入口，不得作为产品主流程宣传。

本项目不是股票投资建议系统。任何报告和中间输出都不得包含买入、卖出、目标价、确定性收益判断、推荐股票或类似投资建议。公司相关内容只能写成“公司业务匹配”“A 股公司关注清单”或等价研究辅助表达，并必须保留证据、资料日期、置信度、反面证据和不确定性。

## 用户分析流程

1. 读取输入：如果用户输入 http/https 链接，先抓取页面或 PDF 正文；如果用户粘贴正文，直接作为本次主政策文本。
2. 正文质量校验：链接抓取或文本清洗后必须确认提取到足够政策正文。若正文过短、像导航页、错误页、登录页、验证码页或非政策内容，必须返回清晰错误，不得生成全 unknown 报告。
3. 主政策分析：Policy Analyst 必须基于用户输入政策本身输出标题、发布主体、发布日期、文号、政策目标、政策措施、约束对象、政策力度和证据。
4. 相似政策检索：本地政策知识库只用于查找相似政策、历史政策或可比政策。相似政策只能用于对比，不得替代用户输入政策的身份和正文。
5. 实施路径分析：Impact Analyst 必须从政策措施推导实施主体、实施行为、产业链环节、行业经营变量和影响公司类型。
6. 行业影响分析：必须说明直接影响、间接影响或潜在影响、影响方向、影响力度、成立条件、风险和不确定性。不得只凭概念板块名称判断受益或受损。
7. 公司业务匹配：Company Matcher 只面向 A 股公司做业务匹配或关注清单。必须说明公司名称、股票代码、匹配产业链环节、相关产品或业务、业务证据、资料年份、来源链接、匹配条件、反面证据、风险和置信度。

## 三 Agent 架构

- Policy Analyst：负责政策身份、发布主体、政策目标、政策措施、约束对象、历史变化、政策力度和证据整理。政策身份以用户输入政策为准。
- Impact Analyst：负责实施主体、实施路径、传导机制、产业链环节、行业经营变量、直接/间接/潜在行业影响和不确定性。
- Company Matcher：负责候选 A 股公司业务匹配、年报或公告证据、政策链条关系、匹配等级、风险和置信度。

三个 Agent 都可以接入 Web Search 作为外部证据通道。Policy Analyst 优先搜索政府官网、主管部门、官方解读、上位政策、实施细则、地方配套、资金安排、试点名单和执行情况。Impact Analyst 优先搜索政府部门、国家统计局、行业协会和权威机构的行业数据。Company Matcher 优先搜索交易所公告、巨潮资讯和公司官网。

## 工具层与 Prompt 层边界

- 工具层负责获取材料：本地政策检索、政策正文读取、元数据查询、网页搜索、MCP 查询、PDF/HTML 读取、结果缓存。
- Prompt、Schema 和 Agent 层负责分析材料：政策解构、政策对比、力度判断、实施路径、行业影响和公司业务匹配。
- 不要把核心判断逻辑优先做成黑盒工具。工具返回证据，Agent 负责解释证据。
- Prompt、Schema、工具和工作流必须分离，方便后续替换模型、向量库、MCP Server 和搜索接口。

## 离线入库与在线 RAG

- 离线政策入库：文件读取、Hash、Policy ID、元数据抽取、Chunk ID、切分、SQLite/FTS 索引、全量政策库构建。
- 在线 Agentic RAG：用户输入读取、主政策校验、相似政策检索、正文读取、外部证据补充、三 Agent 分析和报告生成。
- 用户输入政策不得自动写入本地政策知识库，避免污染离线知识库。
- 若本地知识库没有相似政策，报告必须明确写“未在本地知识库中找到相似政策”，但仍继续完成主政策分析、行业影响和公司匹配。

## MCP 与外部数据边界

- Open-WebSearch：三个 Agent 的外部证据通道，用于最新政策、官方解读、行业数据、公告和新闻。
- CNFinancial：Impact Analyst 用行业、宏观、新闻、板块和行业成分数据；Company Matcher 用公司基本信息、主营业务、财务指标、公告、新闻和竞争对手数据。
- CNINFO：Company Matcher 用于查询和下载候选公司最近两期已经披露的正式年报，并用年报原文验证业务真实性。
- 默认流程必须离线可运行。DeepSeek、Open-WebSearch、CNFinancial 和 CNINFO 都是增强通道；不可用时必须记录不确定性，不能让确定性流程退化成全 unknown。
- CNINFO 年报下载默认跳过，除非任务明确要求启用下载。

## 工程约束

- 修改代码前，先阅读相关模块、Schema、Prompt、测试和现有日志。
- 每次进入下一阶段前，必须先使用 Plan mode 或等价计划步骤明确范围、验证方式和退出条件。
- 每次只完成范围明确的小步任务，避免无关重构和大规模改写。
- 新增功能必须补充对应测试。
- 修改 Python 代码后，必须运行对应测试；修改工作流后，必须运行端到端测试。
- 修改前端页面后，必须启动本地页面并完成前端审查和日志审查。
- 不得删除测试、降低合理断言、伪造固定结果或静默吞掉异常来让测试通过。
- 无法验证的功能必须在结果中明确标记，不得声称已经完成验证。
- 不得随意增加依赖；新增依赖必须服务于当前任务，并写入 `requirements.txt` 或相应依赖文件。
- 不得把 API Key、访问令牌、Cookie 或用户敏感信息写入代码、测试数据或 artifacts。

## 前端审查规则

修改 `app.py` 或任何前端页面后，必须执行前端审查：

- 启动命令示例：`$env:POLICYCHAIN_PORT="8010"; python app.py`
- `app.py` 是本地服务，正常不会自然退出。启动后不得等待进程退出，只检查 8010 端口是否处于 Listen 状态。
- 若 8010 已监听，继续执行页面和 API 检查；验收结束后根据 `Get-NetTCPConnection -LocalPort 8010` 的 `OwningProcess` 杀掉该进程。
- 检查页面加载、输入政策链接、输入政策正文、提交任务、进度条变化、日志变化、报告渲染、错误 URL、长文本显示和控制台错误。
- 验证政策正文提交后，报告不得全 unknown，必须包含政策分析、相似政策对比、实施路径、行业影响、A 股公司业务匹配和不确定性。
- 验证错误 URL 或非政策页面时，页面必须显示清晰错误，日志必须记录失败阶段，不得生成误导性报告。

## 日志审查规则

前端和工作流日志必须结构化返回：

- `time`
- `stage`
- `progress`
- `message`

日志审查必须覆盖完整阶段链路：

- 读取输入
- URL 抓取
- 正文质量校验
- 政策分析
- 相似政策检索
- 行业影响分析
- 公司业务匹配
- 报告生成
- 错误处理

失败任务必须保留错误阶段和错误原因。每次任务结束必须汇报日志审查结果；未完成日志审查时，不能声称前端已验证。

## 常用命令

- 安装依赖：`python -m pip install -r requirements.txt`
- 运行全部测试：`python -m pytest`
- 运行单个测试文件：`python -m pytest tests/test_ingestion.py`
- 兼容测试命令：`python -m unittest discover -s tests -v`
- 启动前端：`$env:POLICYCHAIN_PORT="8010"; python app.py`

## 任务结束汇报要求

每次任务结束必须汇报：

- 创建或修改的文件；
- 运行的测试命令和结果；
- 前端审查结果，如不适用需说明原因；
- 日志审查结果，如不适用需说明原因；
- 发现并修复的问题；
- 剩余问题、当前限制和下一阶段建议。
